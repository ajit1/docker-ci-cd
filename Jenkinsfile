pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        bat 'date'
      }
    }
    stage('test') {
      steps {
        bat 'date'
      }
    }
    stage('finish') {
      steps {
        bat 'dir'
      }
    }
  }
}